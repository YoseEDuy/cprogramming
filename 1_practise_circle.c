#include <stdio.h>

int main()
{
    int radius = 3;
    float pi = 3.1;
    printf("The area of the circle is %f \n", pi * radius * radius);
    int height = 3;
    printf("The volume of cylinder is %f\n", pi * radius * radius);
    return 0;
}