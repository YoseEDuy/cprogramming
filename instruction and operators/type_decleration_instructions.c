#include <stdio.h>

int main()
{
    // int a = 4, b, c; //Type Decleration Instruction
    //                  // float b;   //Type Decleration Instruction
    // b = c = a;
    // printf("The valur of a is %d\n", a);
    // printf("The valur of b is %d\n", b);
    // printf("The valur of c is %d\n", c);

    float a = 1.1;
    float b = a + 8.9;
    printf("The value of b is %.3f\n", b);

    return 0;
}